# Plot audit outputs

Plot audit outputs

## Usage

``` r
plot_audit(
  x,
  type = c("sumdev", "dominance", "entropy", "marker", "smoothness")
)
```

## Arguments

- x:

  An `aegis` object.

- type:

  One of `sumdev`, `dominance`, `entropy`, `marker`, `smoothness`.

## Value

A ggplot object.
