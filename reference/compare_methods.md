# Compare deconvolution methods

Computes pairwise method agreement by cell type and spot-level agreement
across methods.

## Usage

``` r
compare_methods(x)
```

## Arguments

- x:

  An `aegis` object.

## Value

Modified `aegis` object with `x$consensus$comparison` populated.
