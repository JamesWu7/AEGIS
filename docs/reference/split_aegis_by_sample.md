# Split an AEGIS object by sample

Split an AEGIS object by sample

## Usage

``` r
split_aegis_by_sample(x)
```

## Arguments

- x:

  An `aegis` or `aegis_multi` object.

## Value

Named list of per-sample `aegis` objects.
